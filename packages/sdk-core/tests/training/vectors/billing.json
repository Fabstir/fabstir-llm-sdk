{
 "_note": "Interface C.1 at sample pricePerToken 904 (the registered price is set at T7). gross = floor(tokens*price/1000); deposit = max(500000, ceil(gross*1.05)) computed as (gross*105+99)/100. Four cases: worked (divides evenly), roundingBites (floor AND ceil both bite — 949,248 vs a floored 949,247), roundVsFloor (gross fraction .912 — a round-to-nearest gross gives 904,048, floor gives 904,047), minFloorBinds (the 0.5 USDC floor decides).",
 "minDepositMicroUsdc": 500000,
 "cases": {
  "worked": {
   "trainingTokens": 9600000,
   "pricePerToken": 904,
   "grossMicroUsdc": 8678400,
   "depositMicroUsdc": 9112320
  },
  "roundingBites": {
   "trainingTokens": 1000050,
   "pricePerToken": 904,
   "grossMicroUsdc": 904045,
   "depositMicroUsdc": 949248
  },
  "roundVsFloor": {
   "trainingTokens": 1000053,
   "pricePerToken": 904,
   "grossMicroUsdc": 904047,
   "depositMicroUsdc": 949250
  },
  "minFloorBinds": {
   "trainingTokens": 10000,
   "pricePerToken": 904,
   "grossMicroUsdc": 9040,
   "depositMicroUsdc": 500000
  }
 },
 "wireShape": {
  "_note": "Pins the JSON TYPE of the train_accepted/train_complete billing block, which is NOT the same as the numeric inputs above. pricePerToken is a DECIMAL STRING on the wire (u64-and-larger integers ride as strings, per the ground rule; it is a uint256 on chain). The SDK's over-claim guard compares this against the verified on-chain price, so a number-vs-string mismatch would abandon a funded session at slice zero. Added 2026-08-24 at the SDK's request: one line in a vector settles it forever.",
  "train_accepted.billing": {
   "unit": "training-token",
   "tokens": 9600000,
   "pricePerToken": "904"
  },
  "train_complete.billing": {
   "unit": "training-token",
   "tokens": 9600000,
   "pricePerToken": "904"
  },
  "types": {
   "unit": "string",
   "tokens": "number",
   "pricePerToken": "string (decimal)"
  }
 }
}